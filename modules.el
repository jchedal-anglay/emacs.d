(load! :editor
       aggressive-indent
       company
       editorconfig
       expand-region
       flycheck
       hungry-delete
       multiple-cursors
       (parentheses +rainbow)
       project
       :input
       dvorak
       :language
       (cc +lsp)
       clojure
       common-lisp
       (python +lsp)
       :misc
       discord
       exec-path-from-shell
       keyfreq
       :os
       macos
       :themes
       (one-dark +variant)
       :tools
       git
       (lsp +ui)
       :ui
       discoverability
       icons
       (ivy +swiper))
