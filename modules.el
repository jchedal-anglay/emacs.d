(load! :editor
       aggressive-indent
       company
       expand-region
       flycheck
       hungry-delete
       multiple-cursors
       (parentheses +rainbow)
       project
       :input
       dvorak
       :language
       (cc +lsp)
       (python +lsp)
       :misc
       discord
       exec-path-from-shell
       keyfreq
       :os
       macos
       :themes
       (one-dark +variant)
       :tools
       git
       (lsp +ui)
       :ui
       discoverability
       icons
       (ivy +swiper))
